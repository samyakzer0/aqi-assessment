version: 0.2

phases:
  build:
    commands:
      - echo "Starting local artifact configuration check..."
      - pip install -r requirements.txt || true
      - python -m py_compile lambda_function.py
      - echo "Build completed on $(date)"
artifacts:
  files:
    - lambda_function.py
    - requirements.txt